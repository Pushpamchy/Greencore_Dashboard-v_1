import React from "react";
import ReactDOM from "react-dom/client";
import GreencoreDashboard from "./GreencoreDashboard";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<GreencoreDashboard />);
